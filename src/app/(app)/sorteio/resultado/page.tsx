import { SortitionResultScreen } from "@/screens/sortitionResult";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sorteio",
};

export default function SortitionResultPage() {
  return <SortitionResultScreen />;
}
