import { SortitionScreen } from "@/screens/sortition";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sorteio",
};

export default function SortitionPage() {
  return <SortitionScreen />;
}
